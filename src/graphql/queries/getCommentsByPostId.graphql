query GetCommentsByPostId($postId: Int!) {
  getCommentsByPostId(postId: $postId) {
    id
    body
    wasUpdated
    createdAt
    updatedAt
    votes
    creator {
      id
      username
      avatar
    }
  }
}
