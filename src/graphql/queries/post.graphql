query Post($id: Int!) {
  post(id: $id) {
    id
    title
    body
    votes
    createdAt
    updatedAt
    votes
    creatorId
    creator {
      id
      username
      avatar
      email
      createdAt
      updatedAt
    }
  }
}
